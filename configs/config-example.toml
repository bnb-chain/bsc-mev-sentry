[Service]
HTTPListenAddr = "localhost:8545" # The address to listen on for HTTP requests.
RPCConcurrency = 100 # The maximum number of concurrent requests.
RPCTimeout = "10s" # The timeout for RPC requests.

[[Validators]] # A list of validators to forward requests to.
PrivateURL = "https://v1:8545" # The private rpc url of the validator, it can only been accessed in the local network.
PublicHostName = "v1" # The domain name of the validator, if a request's HOST info is same with this, it will be forwarded to the validator.
BidFeeCeil = 0
PayAccountMode = "privateKey" # The unlock mode of the account.
PrivateKey = "59ba8068eb256d520179e903f43dacf6d8d57d72bd306e1bd603fdb8c8da10e8"

[[Validators]] # A list of validators to forward requests to.
PrivateURL = "https://v2:8545" # The private rpc url of the validator, it can only been accessed in the local network.
PublicHostName = "v2" # The domain name of the validator, if a request's HOST info is same with this, it will be forwarded to the validator.
BidFeeCeil = 100000
PayAccountMode = "privateKey" # The unlock mode of the account.
PrivateKey = "59ba8068eb256d520179e903f43dacf6d8d57d72bd306e1bd603fdb8c8da10e8"

[[Builders]]
Address = "0x980A75eCd1309eA12fa2ED87A8744fBfc9b863D5" # The address of the builder.
URL = "http://bsc-builder-1" # The public URL of the builder.

[[Builders]]
Address = "0x980A75eCd1309eA12fa2ED87A8744fBfc9b863D5"
URL = "http://bsc-builder-2"
